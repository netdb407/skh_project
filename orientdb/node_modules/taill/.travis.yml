language: node_js

node_js:
  - "node"

cache:
  directories:
    - "node_modules"
sudo: false

script:
  - yarn test

after_success: 'yarn run coveralls'

notifications:
  email:
    on_success: never
    on_failure: never